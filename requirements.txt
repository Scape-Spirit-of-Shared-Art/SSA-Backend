# FastAPI Backend pentru Reservation Agent
# Core backend dependencies
fastapi==0.118.3
uvicorn[standard]==0.37.0
pydantic==2.11.10
python-multipart==0.0.20

# Agent AI dependencies
browser-use==0.8.0
anthropic==0.69.0
python-dotenv==1.1.1

# Standard library extensions (incluse în Python, dar specificate pentru claritate)
# asyncio - pentru operații asincrone
# time - pentru timestamp-uri
# uuid - pentru generarea session ID-urilor
# json - pentru serializarea datelor
# sys, os - pentru manipularea căilor și sistemului
# typing - pentru type hints
# enum - pentru enum-uri

# Testing dependencies (opționale)
requests>=2.32.5
# pytest>=7.4.0
httpx>=0.28.1

# Development dependencies (opționale)
# black>=23.0.0
# flake8>=6.0.0

# Database dependencies
prisma>=0.13.0
asyncpg>=0.29.0  # PostgreSQL async driver
python-dotenv>=1.0.0  # For environment variables

# npx prisma@5.17.0 generate
# python3 -m prisma generate
# python3 -c "from prisma import Prisma; print('Prisma client imported successfully!')"

# CORS support (inclus în FastAPI, dar specificat pentru claritate)
# fastapi.middleware.cors.CORSMiddleware

# Camera detection dependencies
opencv-python>=4.8.0
ultralytics>=8.0.0
numpy>=1.24.0
Pillow>=9.0.0

# Instrucțiuni de instalare:
# pip install -r requirements.txt
# sau pentru development:
# pip install -r requirements.txt